import { describe, expect, it } from 'vitest';

/** Stubs for the rollout 文件归属探测 requirement (test-plan T-013..T-015). */

describe('processScan', () => {
  it('maps_rollout_file_to_holding_codex_process', () => {
    expect.fail('TODO: implement maps_rollout_file_to_holding_codex_process');
  });

  it('ignores_rollout_fd_held_by_non_codex_process', () => {
    expect.fail('TODO: implement ignores_rollout_fd_held_by_non_codex_process');
  });

  it('skips_processes_that_vanish_during_scan', () => {
    expect.fail('TODO: implement skips_processes_that_vanish_during_scan');
  });
});
